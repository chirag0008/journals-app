spring:
  application:
    name: myFirstProject
  data:
    mongodb:
        uri: 
        auto-index-creation: true
server:
  port: 8080
  servlet:
    context-path: /journal

logging:
  level:
    root: INFO